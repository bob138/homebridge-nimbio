{
  "pluginAlias": "Nimbio",
  "pluginType": "platform",
  "singular": true,
  "strictValidation": false,
  "headerDisplay": "Control your Nimbio cellular gate from Apple HomeKit. Paste a live API key from the Nimbio portal below — Homebridge stores it securely in your config.",
  "footerDisplay": "After saving, restart Homebridge. Your gate will appear in the Apple Home app.",
  "schema": {
    "type": "object",
    "required": ["name", "apiKey"],
    "additionalProperties": false,
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Nimbio",
        "description": "Label for this plugin in Homebridge logs."
      },
      "apiKey": {
        "title": "Nimbio API Key",
        "type": "string",
        "description": "Live API key from the Nimbio portal (starts with nimbio_live_). This is the only secret you need.",
        "placeholder": "nimbio_live_…",
        "options": {
          "hidden": true
        }
      },
      "pulseOnClose": {
        "title": "Also pulse when closing",
        "type": "boolean",
        "default": false,
        "description": "Turn this on if your gate needs a second remote press to close. Most users can leave this off."
      }
    }
  },
  "layout": [
    {
      "type": "flex",
      "flex-flow": "row wrap",
      "items": [
        "name"
      ]
    },
    {
      "key": "apiKey",
      "type": "password"
    },
    "pulseOnClose"
  ]
}
